# Методология бенчмаркинга для light-observable-ts

## Проблема

При сравнении производительности между ветками важно учитывать несколько факторов:
1. **Прогрев кода (JIT warmup)** - V8 JIT-компилятор оптимизирует "горячий" код
2. **Вариативность результатов** - однократные измерения могут сильно отличаться
3. **Условия окружения** - фоновые процессы, состояние CPU, память
4. **Статистическая значимость** - изменения <5% могут быть в пределах погрешности

## Правильная методология

### 1. Прогрев кода (Warmup)
```
2 warmup runs → отбрасываются
5 measured runs → усредняются
```

**Почему это важно:**
- Первые запуски "холодные" - V8 ещё не оптимизировал код
- После нескольких запусков JIT-компилятор оптимизирует горячие пути
- Измерения становятся стабильнее

### 2. Усреднение по множественным запускам

**Минимум 5 запусков** для каждого теста:
- Вычисляем среднее (mean)
- Вычисляем медиану (median) - устойчива к выбросам
- Вычисляем стандартное отклонение (stdDev)
- Вычисляем коэффициент вариации (CV) - показывает стабильность

### 3. Одинаковые условия

✅ **Правильно:**
```bash
# Запускаем бенчмарки на обеих ветках в одной сессии
./benchmark-proper.sh
```

❌ **Неправильно:**
```bash
# Запускаем в разные дни или на разных машинах
git checkout master && npm run benchmark
# ... через день ...
git checkout perf && npm run benchmark
```

## Инструменты

### 1. `benchmark-runner.js` - Запуск с прогревом и усреднением

**Использование:**
```bash
# На текущей ветке
node benchmark-runner.js

# Результат сохраняется в benchmark-results.json
```

**Что делает:**
- 2 warmup запуска (результаты игнорируются)
- 5 measured запусков (усредняются)
- Вычисляет статистику (mean, median, min, max, stdDev, CV)
- Сохраняет результаты в JSON

**Пример вывода:**
```
light-observable - creation and subscription
------------------------------------------------------------
  Mean:   4,200,000 ops/sec
  Median: 4,190,000 ops/sec
  Min:    4,100,000 ops/sec
  Max:    4,300,000 ops/sec
  StdDev: 75,000 ops/sec
  CV:     1.79%
  Raw values: [4,100,000, 4,200,000, 4,190,000, 4,250,000, 4,300,000]
```

### 2. `benchmark-proper.sh` - Полное сравнение двух веток

**Использование:**
```bash
# Автоматически переключается между ветками и запускает по 5 тестов на каждой
chmod +x benchmark-proper.sh
./benchmark-proper.sh
```

**Что делает:**
- Переключается на `master`
- Запускает 5 бенчмарков
- Переключается на `feature/performance-optimizations`
- Запускает 5 бенчмарков
- Усредняет результаты
- Сохраняет в CSV для анализа

### 3. `benchmark-compare.js` - Сравнение результатов

**Использование:**
```bash
# Сначала запускаем бенчмарки на обеих ветках
git checkout master
node benchmark-runner.js
mv benchmark-results.json master-results.json

git checkout feature/performance-optimizations
node benchmark-runner.js
mv benchmark-results.json perf-results.json

# Затем сравниваем
node benchmark-compare.js master-results.json perf-results.json
```

**Что выдает:**
```
SUMMARY TABLE
--------------------------------------------------------------------------------

light-observable-ts Results:
Test                                          Baseline       Optimized      Change
--------------------------------------------------------------------------------
creation and subscription                     4,200,000      4,420,000      📈 +5.24%
эмиссия 100 значений                          878,000        1,050,000      📈 +19.59%
фильтрация и трансформация                    291,000        329,000        📈 +13.06%

PERFORMANCE IMPACT ANALYSIS
Total tests: 3
Significant improvements (≥5%): 3
Significant regressions (≤-5%): 0
Minor changes (<5%): 0

✅ APPROVE: Optimizations show clear performance improvements with no regressions.
```

## Интерпретация результатов

### Коэффициент вариации (CV)

- **CV < 2%**: Отлично - очень стабильные результаты
- **CV 2-5%**: Хорошо - приемлемая вариативность
- **CV 5-10%**: Средне - нужно больше запусков
- **CV > 10%**: Плохо - результаты ненадежны

### Значимость изменений

- **< 5%**: В пределах погрешности, может быть случайностью
- **5-10%**: Заметное улучшение/ухудшение
- **10-20%**: Значительное изменение
- **> 20%**: Очень значительное изменение

### Пример анализа

```
Test: "эмиссия 100 значений"
Master:  878,000 ops/sec (CV: 2.3%)
Perf:    1,050,000 ops/sec (CV: 1.8%)
Change:  +19.59%
```

**Вывод:**
- ✅ Низкий CV на обеих ветках → результаты надежны
- ✅ Прирост +19.59% → значительное улучшение
- ✅ Можно с уверенностью сказать, что оптимизация работает

## Пошаговая инструкция для ревью

### Шаг 1: Установка зависимостей
```bash
npm install
npm run build
```

### Шаг 2: Запуск бенчмарков на master
```bash
git checkout master
npm install
npm run build
node benchmark-runner.js
mv benchmark-results.json master-results.json
```

### Шаг 3: Запуск бенчмарков на performance-optimizations
```bash
git checkout feature/performance-optimizations
npm install
npm run build
node benchmark-runner.js
mv benchmark-results.json perf-results.json
```

### Шаг 4: Сравнение результатов
```bash
node benchmark-compare.js master-results.json perf-results.json
```

### Шаг 5: Анализ

Проверьте:
1. **CV < 5%** на обеих ветках (стабильность)
2. **Изменения > 5%** считаются значимыми
3. **Нет регрессий** (ухудшений) в критических тестах
4. **Среднее улучшение** соответствует заявленному

## Частые ошибки

### ❌ Ошибка 1: Однократный запуск
```bash
npm run benchmark  # Только один запуск!
```
**Проблема:** Результат может быть случайным.

### ❌ Ошибка 2: Сравнение в разное время
```bash
# Понедельник, 10:00
git checkout master && npm run benchmark

# Среда, 15:00
git checkout perf && npm run benchmark
```
**Проблема:** Разные условия окружения.

### ❌ Ошибка 3: Игнорирование CV
```
Test: 1,000,000 ops/sec (CV: 15%)
```
**Проблема:** Высокий CV → результаты ненадежны.

### ❌ Ошибка 4: Без прогрева
```bash
# Запускаем только один раз без warmup
npm run benchmark
```
**Проблема:** Измеряем "холодный" код, JIT ещё не оптимизировал.

## Рекомендации

### Для локальной разработки
```bash
# Быстрая проверка (1 warmup + 3 measured)
node benchmark-runner.js  # Модифицируйте NUM_RUNS=3
```

### Для финального ревью
```bash
# Полная проверка (2 warmup + 5 measured на каждой ветке)
./benchmark-proper.sh
```

### Для CI/CD
```bash
# Запускаем на одном коммите, сохраняем результаты
node benchmark-runner.js
# Сравниваем с baseline из артефактов
node benchmark-compare.js baseline.json current.json
```

## Дополнительные ресурсы

- [V8 Performance Tips](https://v8.dev/blog/turbofan-jit)
- [Benchmark.js Best Practices](https://benchmarkjs.com/)
- [Statistical Significance in Benchmarking](https://en.wikipedia.org/wiki/Statistical_significance)

## Контрольный список для ревьюера

- [ ] Бенчмарки запущены на обеих ветках
- [ ] Использовано ≥5 запусков на ветку
- [ ] Прогрев кода выполнен (warmup runs)
- [ ] CV < 5% для критических тестов
- [ ] Изменения ≥5% считаются значимыми
- [ ] Нет значимых регрессий
- [ ] Результаты задокументированы
- [ ] Сравнение выполнено в одинаковых условиях
